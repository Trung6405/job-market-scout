# Work summary — 2026-07-22

A high-output day: **38 commits**, ~**3,800 insertions / 1,200 deletions** across
**131 file-touches**. Work fell into five themes, listed newest-first.

---

## 1. Requirements: LLM decides met/unmet (evening)

Replaced the brittle exact-string gap check with an LLM equivalence judgment,
so prose requirements like "Proficient in one or more programming languages"
are correctly marked met against a profile listing Python/JavaScript.

| Commit | Summary |
|---|---|
| `5a309c3` | docs(spec): record the met/unmet amendment |
| `13e3624` | feat(advisor): let the extraction LLM decide requirement met/unmet |
| `9c23a3d` | feat(advisor): render Markdown job descriptions to HTML |

---

## 2. Profile as the single candidate source (major initiative)

Migrated the whole pipeline off `resume.txt` onto a structured `profile.json`,
spec-through-cleanup: scorer, briefing, and advisor all now read a rendered
profile, and gap detection always runs.

| Commit | Summary |
|---|---|
| `5e15fef` | Merge: profile as the single candidate source |
| `846809c` | ci: move scheduled run to 05:00 UTC |
| `476e7f7` | chore: remove remaining resume references |
| `41e7025` | docs(plan): mark phase 3 and plan complete |
| `c56057b` | ci: drop resume placeholder and render steps |
| `8096c22` | chore: remove resume.txt, compose mount, env example |
| `ce0f49c` | refactor(config): drop resume_text; profile is the only source |
| `37982fd` | docs: mark phase 2 complete; record load_profile amendment |
| `720fb38` | feat(pipeline): require profile; gap detection always runs |
| `163f716` | feat(briefing): use the rendered profile |
| `5895b4f` | feat(scorer): score against the rendered profile |
| `823ef15` | docs(plan): mark phase 1 complete |
| `9f21cae` | feat(config): load profile into Settings (additive) |
| `1186a1c` | feat(profile): add render_profile_text renderer |
| `c59168b` | docs(plan): implementation plan for profile-as-candidate-source |
| `24de66e` | docs(spec): profile as the single candidate source |
| `225429f` | feat(agent): RUN_DATE override + richer example profile |
| `4eb4f47` | docs(readme): profile.json ships a tracked placeholder |

---

## 3. Azure VM CI/CD deployment (Bicep IaC + GitHub Actions)

Landed, reverted, and re-landed the deployment stack, then hardened CI so the
test/deploy gates pass reliably.

| Commit | Summary |
|---|---|
| `5abafe7` | Merge feat/azure-vm-cicd-deploy into integ-relanding |
| `aa4bb13` | Reapply Azure VM CI/CD deployment (#12) |
| `7a2371a` | feat(infra): input bicep parameters |
| `e7148b9` | ci(deploy): gate the deploy job to main only |
| `3a08afa` | fix(ci): give the test job a Postgres so the gate passes |
| `08bfdd2` | test(agent): stub load_profile in persist tests |
| `4699764` | test(agent): configure Gmail creds so briefing path is hermetic |
| `5a2090d` | fix(ci): materialize resume.txt from template before pytest |
| `d18b35f` | fix(deploy): render resume on VM; harden .env heredoc |
| `15013f8` | Merge main into feat/azure-vm-cicd-deploy |
| `edafc18` | Revert Azure VM CI/CD deployment (#12) |
| `862bccb` | feat: Azure VM CI/CD deployment (#12) — *Phuc Dang* |

---

## 4. Web serving & dashboard

| Commit | Summary |
|---|---|
| `c7ea64f` | fix(web): mount hello outside the read-only dashboard root |
| `11c5fc8` | feat(web): serve dashboard at `/`, move hello to `/hello` |

---

## 5. Advisor report + reliability fixes

| Commit | Summary |
|---|---|
| `3d0317a` | fix(advisor): close phase-4 template gaps; fix listings_scored/day-nav bugs |
| `44d5e12` | fix(mcp): skip progress notification when no progressToken present |
| `3bc86cc` | fix(agent): skip briefing email when Gmail creds not configured |

---

*Generated from `git log` for 2026-07-22. Author: Trung6405, except where noted.*
